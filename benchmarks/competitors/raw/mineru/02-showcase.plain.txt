Feature Showcase

Every inline decoration: strong, emphasis, underline, strikethrough, highlighted text, and bold italic. A link with a footnote.1

Superscript note and subscript { \mathrm { i n d e x } } both render as decorations.

An image resolves relative to the .crv file, via the injected  :

 Light

 Medium

 Dark

 French

Figure 1: the four roast stages, lightest to darkest Table with row and column spans 

The ^ in the last row extends the “APAC” cell down two rows; the < merges into the cell on its left.

Critic markup

The quick brown fox lazy jumps over the doglog

Admonition

Note

This is an admonition block - rendered as a tinted container.

Plain text should render beautifully and stay readable at the source.

Design principle

1. Inline footnote content, rendered as an endnote.↩
