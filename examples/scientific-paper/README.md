# Scientific paper

This synthetic two-column page combines reading order, display math, a footnote,
and a compact results table. The ground truth uses typed math and an inline footnote.

Carve retains those meanings directly. Common Markdown can display the table but
has no standard footnote or math model; the comparison uses widespread extensions,
which are not portable across all renderers.
