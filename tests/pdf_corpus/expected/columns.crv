# Column study

Left one carries enough text for a column. Left two remains in reading order. Left three closes the first column.

Right one carries enough text too. Right two follows its own first line. Right three closes the second column.
